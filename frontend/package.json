{
  "name": "goldenhour-frontend",
  "version": "0.0.0",
  "private": true,
  "description": "GoldenHour frontend — repository foundation (Ticket 01). Application code is added in a later ticket.",
  "scripts": {
    "dev": "node ../backend/node_modules/typescript/lib/tsc.js -p tsconfig.json --watch",
    "build": "node ../backend/node_modules/typescript/lib/tsc.js -p tsconfig.json",
    "typecheck": "node ../backend/node_modules/typescript/lib/tsc.js -p tsconfig.json --noEmit",
    "test": "node ../backend/node_modules/typescript/lib/tsc.js -p tsconfig.json --noEmit"
  }
}